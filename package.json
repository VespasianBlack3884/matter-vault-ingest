{
  "name": "matter-vault-ingest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "setup": "node --experimental-strip-types src/vault_setup.ts",
    "intake": "node --experimental-strip-types src/intake_cli.ts",
    "serve": "node --experimental-strip-types src/matter_service.ts"
  },
  "dependencies": {
    "openai": "^4.77.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
